stages:
  prepare:
    cmd: python -m src.data.make_dataset
    deps:
      - github-issues-dataset/github_issues_dataset.parquet
      - src/data/preprocess.py
      - src/data/taxonomy.py
      - src/data/make_dataset.py
    outs:
      - data/processed/issues_clean.parquet
  split:
    cmd: python -m src.data.make_splits
    deps:
      - data/processed/issues_clean.parquet
      - src/data/make_splits.py
    params:
      - split
    outs:
      - data/splits
